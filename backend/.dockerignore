# Committed sample images — tens of MB of build context for no reason.
# The runtime volume supplies this directory.
images/

# Build output
bin/
*.exe

# Never let secrets into a build layer
.env
.env.*

*.log
.DS_Store
